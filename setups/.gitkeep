量測擺位(setup)存在這個資料夾,一個擺位一個 JSON。

⚠️ 這個資料夾**要進版控**,跟 plans/ 一樣。

不是因為它是程式碼,而是因為它刪掉就沒了 —— 一個擺位是「這一次量測,
房間裡每個東西站在哪裡」,那是對過現場、花時間調出來的數字,不是執行期
的暫存檔。誤刪過一次就知道重建有多痛(而且你不會記得 19 根軸各是多少)。

所以 .gitignore 裡**刻意沒有** setups/。要新增擺位就一起 commit:

    git add setups/
    git commit -m "setup: 近場 0.5 m"

這個檔案本身只是讓空資料夾也能進版控(git 不追蹤空目錄)。

跑測試時不要用這個資料夾 —— 測試開場會把擺位刪光。用:

    python app.py --setups-dir <暫存目錄>

詳見 soundproofroom/README.md 的「擺位(setup)」。
